k=1;
tau=10;
delay=30;
runPlotDelay(k, tau, delay, "FirstOrderModelDelayed", "FOMDelay1")

delay=10;
runPlotDelay(k, tau, delay, "FirstOrderModelDelayed", "FOMDelay2")

delay=-10;
runPlotDelay(k, tau, delay, "FirstOrderModelDelayed", "FOMDelay3")
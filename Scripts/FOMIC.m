k=3;
tau=10;
IC=2;
delay=0;
runPlotIC(k, tau, delay, IC, "FirstOrderModelIC", "FOMIC1")

IC=6;
runPlotIC(k, tau, delay, IC, "FirstOrderModelIC", "FOMIC2")

IC=6;
delay=5;
runPlotIC(k, tau, delay, IC, "FirstOrderModelIC", "FOMIC3")